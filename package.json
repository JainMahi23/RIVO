{
  "name": "rivo",
  "private": true,
  "version": "0.1.0",
  "description": "RIVO - Hyper-local AI-powered business advisory & financial planning platform",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:ml": "cd ml-service && uvicorn app.main:app --port 8000 --reload",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ml\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
