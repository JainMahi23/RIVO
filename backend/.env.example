# Server
PORT=5000
NODE_ENV=development

# Database (fill in once Atlas cluster exists — never commit real values)
MONGO_URI=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# CORS
CLIENT_ORIGIN=http://localhost:5173
