// Placeholder i18n setup. RIVO's target users are rural/semi-urban and
// often more comfortable in a regional language than English, so
// multi-language support is a first-class concern, not an afterthought.
// Wire up i18next (or similar) here when localization work starts.
export const supportedLocales = ["en", "hi"];
export const defaultLocale = "en";
